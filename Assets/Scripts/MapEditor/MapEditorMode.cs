namespace MapEditor {
    /**
     * Enum of modes in Map Editor
     */
    public enum MapEditorMode {
        None, // Disabled/Default
        Wall,
        Prop,
        Difficulty,
        Event
    }
}